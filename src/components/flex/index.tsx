import { FlexBox, FlexBoxProps } from 'components/flex/FlexBox';
import {
  FlexContainer,
  FlexContainerProps,
} from 'components/flex/FlexContainer';

export type { FlexBoxProps, FlexContainerProps };
export { FlexBox, FlexContainer };
